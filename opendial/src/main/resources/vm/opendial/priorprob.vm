#set($descriptor=$MAP.get("descriptor"))
#set($taskCode=$descriptor.taskCode)
#set($initCurrentStep=$descriptor.initCurrentStep)
#set($welcomeUtterance=$!descriptor.welcomeUtterance)
#set($finalUtterance=$!descriptor.finalUtterance)
#set($closeUtterance=$!descriptor.closeUtterance)
#set($prevExtendProb=$descriptor.prevExtendProb)
#set($prevConfirmExtendProb=$descriptor.prevConfirmExtendProb)
#set($prevDisconfirmExtendProb=$descriptor.prevDisconfirmExtendProb)
#set($pubGroundUtil=$descriptor.pubGroundUtil)
#set($pubConfirmUtil=$descriptor.pubConfirmUtil)
#set($pubAskRepeatUtil=$descriptor.pubAskRepeatUtil)
#set($qnas=$descriptor.qnas)
#set($judgmentQnas=$descriptor.judgmentQnas)
#set($slotQnas=$descriptor.slotQnas)
<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<domain>
#parse("/vm/opendial/0_priorprob.vm")
</domain>